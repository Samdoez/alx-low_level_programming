#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{       int loww;
for (loww = 0; loww <= 9; loww++)
{	putchar(loww);
}
printf("\n");
return (0);
}
