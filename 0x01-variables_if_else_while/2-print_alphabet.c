#include<stdio.h>
#include<stdlib.h>
#include<time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	char low;

for (loww = 'a'; loww <= 'z'; loww++)

	putchar(loww);
	prinf("\n");

return (0);
}
